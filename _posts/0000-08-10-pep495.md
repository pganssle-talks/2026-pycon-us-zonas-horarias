<div class="bullet-container medium-code code-indented">

# PEP 495: Disambiguación de horas locales

* Introducido en Python 3.6 para resolver el problema de las horas ambiguas.
* Introduce el atributo `fold` en `datetime`.
* Cambios en la comparación de `datetimes` conscientes en horas ambiguas.

<div class="bullets-with-header">

Estar en un lado u otro de la transición es una **propiedad del `datetime`**:

<div class="small-spacer"></div>

<div style="display: flex; flex-direction: column;  justify-content: space-around; min-height: 31dvh;">

<div
    class="fragment disappearing-fragment nospace-fragment fade-out"
    data-fragment-index="0">

```python
>>> print_tzinfo(datetime(2004, 10, 31, 1, 30, tzinfo=NYC))          # fold=0
2004-10-31 01:30:00-0400
    tzname:   EDT;      UTC Offset:  -4.00h;        DST:      1.0h

>>> print_tzinfo(datetime(2004, 10, 31, 1, 30, fold=1, tzinfo=NYC))
2004-10-31 01:30:00-0500
    tzname:   EST;      UTC Offset:  -5.00h;        DST:      0.0h
```

</div>
<img
    src="images/fold_495.svg"
    style="height: 31dvh; margin: 0;"
    class="fragment nospace-fragment fade-in" data-fragment-index="0"
    alt="Diagrama del modelo de resolución de horas ambiguas del PEP 495, mostrando que fold desplaza el momento en que se aplica la etiqueta 'DST'."
/>

</div>
</div>

**N.B.:** `fold=1` representa la *segunda* instancia de un `datetime` ambiguo.

<div class="small-spacer"></div>

</div>

Notes:

La solución con la que han dado en Python 3.6 era simplemente añadir un solo byte de información a la parte naíf del datetime, y ese es el atributo fold.

La idea es que tienes este atributo que puedes usar para especificar cómo resolver de qué lado de una transición quieres estar, como en pytz, pero con la diferencia de que aquí la información no está guardada en el `tzinfo`, sino en el propio `datetime`, así que se puede usar de manera *lazy*.

➡️ Y como funciona es que `fold=0` es la *primera* instancia de una hora ambigua, y `fold=1` es la *segunda*.

[45s; T: 18m 15s]

--


<div class="centered-container">

<div>

## <tt>pytz</tt>

<img
    src="images/fold_pytz.svg"
    style="height: 31dvh"
    alt="Diagrama del modelo de resolución de horas ambiguas de pytz, mostrando que is_dst cambia el periodo total al que se aplicaría la etiqueta 'DST'."
/>

</div>

<div class="small-spacer"></div>
<div class="small-spacer"></div>

<div>

## PEP 495

<img
    src="images/fold_495.svg"
    style="height: 31dvh"
    alt="Diagrama del modelo de resolución de horas ambiguas del PEP 495, mostrando que fold desplaza el momento en que se aplica la etiqueta 'DST'."
/>

</div>

</div>

Notes:

Y eso es un poco diferente a la manera en que `pytz` lo hace, porque a `pytz` no le importa cuál viene primero.

Lo que `is_dst` quiere decir es que si hay una hora ambigua, puedes escoger si quieres el lado de verano o el lado estándar, pero lo que `fold` quiere decir es: "Vale, si hay una hora ambigua, ¿mantenemos el desplazamiento que se aplicaba antes de la transición o después?".

Y resulta que esta forma es un poco más simple de implementar, y también está mejor definida, porque, como ya hemos aprendido, a veces estas transiciones ocurren por motivos distintos a un cambio de hora de verano, así que puedes tener hora estándar o hora de verano en *ambos lados*. Con `fold` ya no es ambiguo porque solo tienes que saber qué desplazamiento ocurrió antes y cuál después.

[1m; T: 19m 15s]
